import 'package:flutter/material.dart';

class CalendarDatePicker extends StatelessWidget {
  const CalendarDatePicker({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('This is a calendar date picker.');
  }
}
