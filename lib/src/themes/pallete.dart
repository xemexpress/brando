import 'package:flutter/material.dart';

class Pallete {
  // static const Color primary = Color.fromRGBO(250, 216, 189, 1);
  static const Color primary = Color.fromRGBO(253, 218, 190, 1.0);
  static const Color onPrimary = Color.fromRGBO(0, 0, 0, 1);
  static const Color secondary = Color.fromRGBO(253, 218, 190, 1.0);
  static const Color surfaceVariant =
      Color.fromRGBO(190, 190, 190, 1); // For hint text color

  // Light Reference
  //
  //
  //
  //
  // static const Color primary = Color.fromRGBO(250, 216, 189, 1.0);
  static const Color primaryVariant1 = Color.fromRGBO(145, 126, 111, 1.0);
  static const Color primaryContainer = Color.fromRGBO(246, 228, 214, 1.0);

  static const Color secondaryVariant = Color.fromRGBO(41, 37, 34, 1.0);
  static const Color background = Color.fromRGBO(255, 255, 255, 1.0);
  static const Color surface = Color.fromRGBO(255, 255, 255, 1.0);
  static const Color error = Color.fromRGBO(176, 0, 32, 1.0);
  // static const Color onPrimary = Color.fromRGBO(255, 255, 255, 1.0);
  static const Color onSecondary = Color.fromRGBO(0, 0, 0, 1.0);
  static const Color onSurface = Color.fromRGBO(0, 0, 0, 1.0);
  static const Color onBackground = Color.fromRGBO(0, 0, 0, 1.0);
  static const Color onError = Color.fromRGBO(255, 255, 255, 1.0);

  // Dark Reference
  //
  //
  //
  //
  // Additional colors for the dark theme
  static const Color primaryDark =
      Color.fromRGBO(180, 138, 117, 1.0); // Darker shade of primary
  static const Color onPrimaryDark =
      Color.fromRGBO(255, 255, 255, 1.0); // For contrast against primaryDark

  // Example dark theme background and surface colors
  static const Color backgroundDark = Color.fromRGBO(18, 18, 18, 1.0);
  static const Color surfaceDark = Color.fromRGBO(38, 38, 38, 1.0);

  // Adjusting onSurface and onBackground for dark theme readability
  static const Color onSurfaceDark = Color.fromRGBO(255, 255, 255, 1.0);
  static const Color onBackgroundDark = Color.fromRGBO(255, 255, 255, 1.0);
}
